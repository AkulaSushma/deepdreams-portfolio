# DeepDreams Portfolio — Ocean Engine Rebuild + Showcase Restructure

## What's actually broken right now
`js/ocean.js` (the file powering *all* the background behavior you described) is **syntactically corrupted and throws on load**. The canvas never starts, so today there are no fish, no god rays, no marine snow, no jellyfish, and no scroll-descent. Confirmed errors:
- Lines 133–135: `const gr = …` declared 3× → `Identifier 'gr' has already been declared` ReferenceError
- Lines 208–213: a `/* … */` comment swallows the closing of `JELLY_TINTS` *and* the `const jellies = Array.from(...)` declaration → line 214's `fx: rnd(...)` is orphaned, `jellies` undefined
- Line 236: `const a = j.vis * (…); (0.28 + curDepth * .5);` → dangling expression statement
- Line 117: `anchor()` references `t` before it's declared at line 285 → temporal-dead-zone error when anchor is called in the fish loop (line 309)

So this isn't a polish task — it's a rewrite. Everything you described (school in the upper-right light, touch-scatter-and-return, smooth descent to an abyss with glowing jellies at the bottom) gets built on a corrected engine.

---

## A) Ocean engine — full rewrite of `js/ocean.js`

Single fixed full-screen `<canvas id="ocean">` (already in `index.html`), fixed in a permanent background layer. Self-contained physics in one file. Mobile-aware (particle counts and DPR cap). Honors `prefers-reduced-motion` (renders a single static frame, no rAF loop).

**Scroll descent (`depthT 0→1`) wired to Lenis** — not the raw window scroll. `app.js` already imports Lenis; I'll call `window.oceanScroll(y)` from the same `onScroll` Lenis hook `app.js` already runs (line 96), so descent is smooth *and* matches Lenis's eased wheel. This replaces the file's plan to listen on its own.

**Layers, far→near (parallax sells the dive):**
1. **Depth gradient + corner light source.** A single radial glow pinned to the top-right corner dims from `light=1` (sunlit surface, blue #1d4f7f→#070C18) to `light=0` (abyss near-black, #01030a). Eased via `curDepth`. Breathing alpha.
2. **God rays.** 7 fans down-left from the surface light, cached linear gradients, swaying + pulsing. Recede to near-zero past 60% depth (sunlight is gone in the deep).
3. **Marine snow.** 60 drifting motes with z-depth; nearest move fastest, scroll stretches them into motion-blur streaks (the file already has this idea — keep it). Fades below ~40% depth (less suspended matter at depth).
4. **The school (this is the centerpiece):** small colorful flock drifting in the upper-right lit water.
   - Boids-lite steering: **seek** (toward anchor) + **separate** (short-range repulsion from each other) + mild **wander** (two-offset sine). Forces integrated as velocity with capped max-speed and min-speed (so fish never freeze), lerped positions.
   - **Touch interaction (scatter-and-return):** `pointerdown` within a fright radius applies an outward **flee force** proportional to closeness, sets `panic` (decays exponentially). While panic is high, *seek weakens* (they flee first) and tail-beat frequency doubles. As panic decays, seek re-dominates and the fish home back to the upper-right anchor, decelerating gently as they arrive (no snap). This matches the file's intent but with proper flocking, not just additive velocity nudges.
   - **Anchor follows descent:** school home-base shifts DOWN with `curLift` so scrolling down makes them **dive away below the screen**, and the fish visibly recede (scale & alpha fall with `curDepth`). Scroll back up → home rises and fish cruise back up. Body direction (`atan2(vy,vx)`) flips to face travel, mirrored for left-facing fish.
   - Palette: coral / studio-gold / teal / glint-blue / rose (your existing palette — keep).
5. **Bioluminescent jellyfish at the bottom** (appears past ~45% depth, full strength at ~80%): translucent pulsing bells, sine-ribbon tentacles, radial halo, tints (ice-blue / violet / aqua / rose). Each independently fades in on its own `entry` threshold. Strictly visible only deep — matches your "ocean bottom, jellyfish found" cue.

All time-stepped against `dt` (not frame count) so motion matches wall-clock on any device. Honors visibility API (pause rAF when tab hidden).

## B) Hero + copy reframing (`index.html`, `css/style.css`)

Reframe hero from "tribute films, born from a photo" (memorial-only) to **memorial + milestone/blessing**.
- Hero copy: "Emotional tribute films for the moments that matter most — *born from a single photo.*" Span both the memorial use (a person remembered) and the celebration use (a late father blessing a wedding/birthday) in the supporting subhead.
- Keep the hero featured film (your best tribute work) as the cinematic lead.
- Add a tasteful South-Indian cultural signifier (subtle marigold-gold + temple-arch motif in section dividers, not loud) consistent with your wedding-invite brand (you already use Cormorant + gold + temple doors in `wedding/invite.html`).

## C) Showcase restructure (`index.html`, `js/app.js`, `js/config.js`, `css/style.css`)

**1. Featured strip (above the tabs)** — a full-width "featured work" band:
- 1 hero tribute film (cinematic, large)
- 2 wedding-invitation videos (side-by-side cards)
- 1 wedding website (device mockup, live iframe + "Visit Site" button)
- Clearly labeled "Featured Work" — sets expectations before the visitor dives into the full categorized collection below.

**2. Tabbed Work section** — chips filter: **Tributes · Invitations · Name Reveals · Wedding Sites · AI Builds**.
- Tributes / Invitations / Name Reveal videos → YouTube-nocookie lightbox (reuse the existing `openLB`).
- Wedding Sites / AI Builds → **device mockups** (phone+desktop frame) embedding the live site in an `<iframe>` + "Visit Site ↗" button in a new tab. If a site blocks framing (X-Frame-Options), I detect the iframe load failure and fall back to a poster + Visit button so nothing looks broken.

**3. Data wiring via `config.js`** (the only file you edit — your README document):
- Add new config fields, each overridable-by-Google-Sheet (same pattern you already use for Tributes):
  - `INVITATION_VIDEOS` / Sheet tab `Invitations`
  - `NAME_REVEAL_VIDEOS` / Sheet tab `NameReveals`
  - `WEDDING_SITES` / Sheet tab `Websites` (couple · url · note)
  - `AI_BUILDS` / Sheet tab `AIBuilds` (title · url · note)
  - `FEATURED = { tribute, invitations[], website }` for the top strip
- Each loads **live from the Sheet** if a tab exists, **else** falls back to the arrays in `config.js`, **else** to demo placeholder data so the section always looks complete. You paste real links into `config.js` later — no code edits, no redeploy.

**4. App.js refactor**
- Keep `loadVideos()` but generalize into `loadCollection(sheetTab, fallbackArray, targetTabId, renderer)`.
- Add `renderVideoGrid()` (existing card+lightbox) and `renderSiteMockups()` (new device-mockup renderer).
- Keep ScrollTrigger re-reveal on freshly-rendered cards.
- Hook Lenis's existing `onScroll` to also feed `window.oceanScroll(y)` (currently `ocean.js` would self-listen — move that dispatch here so it's piped through Lenis smoothness).

## D) Cleanup
- Delete `scratch_jelly_code.txt` (dead debugging artifact, contains a Python transcript scanner — irrelevant).
- `firebase-debug.log`, `wedding_qr.png` — leave untouched (not referenced by the main site; you may want them elsewhere).

---

## Files I'll edit

| File | Change |
| :--- | :--- |
| `js/ocean.js` | **Full rewrite** — fix all syntax corruption + implement the layers above. Self-contained. |
| `js/app.js` | Generalize gallery → multi-collection tabs, device-mockup renderer, pipe Lenis scroll → `oceanScroll()`. |
| `js/config.js` | Add `INVITATION_VIDEOS`, `NAME_REVEAL_VIDEOS`, `WEDDING_SITES`, `AI_BUILDS`, `FEATURED` with documented placeholders + Sheet-tab names. |
| `index.html` | Reframe hero copy; add Featured strip block; replace single `#gallery` with tabbed multi-collection layout + `#filters`; device-mockup templates inside lightbox; new sections markers for scroll-depth handoff. |
| `css/style.css` | Featured strip + device-mockup + tabs + dark-abyss contact treatment + subtle South-Indian dividers. Palette stays your blue+gold. |
| `scratch_jelly_code.txt` | Delete (dead scratch file). |

No new dependencies — uses Lenis + GSAP + ScrollTrigger you already load. No backend. No redeploy needed when you add real video links (config/Sheet-driven, exactly as your README promises).

---

## Risks / notes I'm flagging up front
- **iframe embedding of live wedding sites:** some hosts send `X-Frame-Options: DENY` or `frame-ancestors`. I'll add a load-failure fallback (poster + Visit button) so the section degrades gracefully — but a real screenshot poster is the only fully-safe visual, and I can't auto-capture one from here. You may later add a screenshot .jpg per site as the poster; the layout reserves a slot for it.
- **Performance:** a 60-mote scene + 9 fish + jellies + rays on a phone is fine at the 1.5-DPR cap the file already uses; I'll keep that. Reduced-motion users get a static frame.
- **Per favore / cultural specificity:** "South Indian culture" is broad — I'm reading your existing brand (`wedding/invite.html`: temple doors, marigold, Cormorant + Great Vibes + Noto Serif Telugu) as the style anchor. If you wanted a specific regional palette (Tamil/Kerala temple gold vs. Andhra marigold, etc.), now's the moment to mention it.