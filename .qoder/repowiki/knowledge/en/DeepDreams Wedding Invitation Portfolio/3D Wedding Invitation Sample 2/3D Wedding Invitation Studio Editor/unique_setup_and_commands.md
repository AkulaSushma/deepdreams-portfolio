Run `create.html` directly in a browser (the file includes a `file://` protocol shim that rewrites versioned asset URLs to bare filenames so the editor works offline). A published design requires an activation code obtained from the studio via WhatsApp; the editor calls `window.DD_PUBLISH.publish({ token, template: 'sample2', content })` which must be provided by the hosting environment.